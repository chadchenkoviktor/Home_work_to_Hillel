// Task 7.1
task = 'task № 7.1';
console.log(task);

let str = '';

function delitLeterFromWord (word, leter = []) {
     alert(str += `${word}`);
     console.log(Array.isArray(leter));
     console.log(leter);
     
}

delitLeterFromWord(prompt('Введите слово или фразу'), prompt('Введите букву которую будем удалять'));